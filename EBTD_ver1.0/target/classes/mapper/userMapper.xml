<?xml version="1.0" encoding="UTF-8"?>

<!-- 김아름 -->
<!-- UserDAO 전용 Mapper: user관리 sql문 명시 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ebtd.www.dao.I_UserDao">

	                <!-- mybatis-config.xml에 별칭설정 or Bean에 @Alias("user") -->
	<select id="getUserList" resultType="user">
		SELECT * FROM U
	</select>
	
	<select id="getUserHistoryList" resultType="userReserveHistory">
		SELECT URH.URH_NO, URH.U_USERNAME, URH.B_NO,B.C_USERNAME, URH.URH_DATE, URH.S_NOSTART, URH.S_NOLAST,URH.URH_STATUS 
		FROM URH LEFT JOIN B ON URH.B_NO = B.B_NO
	</select>
	

</mapper>
